# Image-to-Latex-code
A python desktop application (UI) that can reads any raster image (JPG, PNG, etc) and converts it into a fully Latex/Tikz code file, with the porpose of not need of the original image.
